name: go-format

on:
  workflow_call:

permissions:
  contents: read

jobs:
  go-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: packages/engine-wasm/go.mod
          cache-dependency-path: packages/engine-wasm/go.sum

      - name: Install treefmt
        run: |
          curl -fsSL https://github.com/numtide/treefmt/releases/download/v2.1.1/treefmt_2.1.1_linux_amd64.tar.gz \
            | sudo tar -C /usr/local/bin -xz treefmt

      # Pinned: @latest formatter drift breaks check-formatted on unchanged code.
      # Keep these in sync with the versions used to format the repo.
      - name: Install Go formatters
        run: |
          go install mvdan.cc/gofumpt@v0.10.0
          go install github.com/daixiang0/gci@v0.14.0

      - name: Install shfmt
        run: go install mvdan.cc/sh/v3/cmd/shfmt@v3.12.0

      - name: Setup just
        uses: extractions/setup-just@v2

      - name: Check formatting
        run: just check-formatted
