# ── Node / Bun ───────────────────────────────────────────────────────────────
node_modules/
.bun/

# ── Frontend build output ────────────────────────────────────────────────────
apps/editor-web/dist/
*.tsbuildinfo
apps/editor-web/public/engine.wasm
apps/editor-web/public/wasm_exec.js

# ── Go build artifacts ────────────────────────────────────────────────────────
packages/engine-wasm/engine
packages/engine-wasm/coverage.out
packages/engine-wasm/coverage.html
*.test
# Failing PSD fixture comparisons dump their artifacts here; the leading
# underscore keeps the directory invisible to the go tool and to go:embed.
packages/engine-wasm/internal/io/psdfixture/_dump/

# Fixture tooling scratch output and its pinned venv (`just fixtures-setup`)
__pycache__/
.fixtures-out/
.venv-psdfixtures/

# ── OS / Editor ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ── Env / Secrets ────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── AI Agents ────────────────────────────────────────────────────────────────
.codex

# Go workspace file — local dev only (points at sibling ../agg_go);
# deploy resolves agg_go from the published module in go.mod/go.sum.
go.work
go.work.sum
