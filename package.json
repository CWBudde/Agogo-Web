{
  "name": "agogo-web",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/editor-web dev",
    "build": "just build",
    "test": "just test",
    "lint": "just lint"
  },
  "devDependencies": {
    "lefthook": "^2.1.4"
  }
}
