version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

    - revive
  settings:
    revive:
      rules:
        - name: file-length-limit
          arguments:
            - max: 1500
              skip-comments: false
              skip-blank-lines: false
  exclusions:
    rules:
      # Functions registered via js.FuncOf are called from JS, not Go — suppress false "unused" warnings.
      - path: cmd/engine/main\.go
        linters: [unused]
      # Long table-driven test files are acceptable; the length limit targets production code.
      - path: _test\.go$
        linters: [revive]
formatters:
  enable:
    - gofumpt
    - gci
